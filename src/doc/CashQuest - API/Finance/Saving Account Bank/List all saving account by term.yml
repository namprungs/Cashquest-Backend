info:
  name: List all saving account by term
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/banks/term/{{termId}}/savings-configs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
