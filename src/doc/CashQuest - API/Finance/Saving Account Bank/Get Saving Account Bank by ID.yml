info:
  name: Get Saving Account Bank by ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/banks/{{bankId}}/savings-configs"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
