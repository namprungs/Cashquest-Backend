info:
  name: Delete Bank by ID
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/banks/{{bankId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
