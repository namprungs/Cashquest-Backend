info:
  name: Get Bank Stats
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/banks/{{bankId}}/statistics"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
