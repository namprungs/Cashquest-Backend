info:
  name: mature trigger
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/fixed-deposits/process-matured"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
