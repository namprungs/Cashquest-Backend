info:
  name: Get Accounts by Student
  type: http
  seq: 5
http:
  method: GET
  url: "{{baseUrl}}/savings-accounts/student/{{studentProfileId}}"
  auth: inherit
settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
