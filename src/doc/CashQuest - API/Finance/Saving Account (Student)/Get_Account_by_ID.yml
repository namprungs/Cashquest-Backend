info:
  name: Get Account by ID
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/savings-accounts/{{savingsAccountId}}"
  auth: inherit

runtime:
  variables:
    - name: accountId
      value: a6c74d09-1697-44be-bdf8-45100564241c

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
