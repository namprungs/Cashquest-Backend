info:
  name: Lists all fixed deposits for a specific student
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/fixed-deposits/student/:studentProfileId"
  params:
    - name: studentProfileId
      value: d50f4bda-eea2-4a26-b867-e8edb489969a
      type: path
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
