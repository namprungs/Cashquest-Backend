info:
  name: Process Matured Deposits
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/fixed-deposits/process-matured"
  body:
    type: json
    data: "{}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
