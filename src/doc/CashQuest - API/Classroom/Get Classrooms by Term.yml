info:
  name: Get Classrooms by Term
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/academic/terms/{{termId}}/classrooms"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
