info:
  name: Get Students in Classroom
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/academic/classrooms/{{classroomId}}/students"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
