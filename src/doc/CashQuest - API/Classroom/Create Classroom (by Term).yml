info:
  name: Create Classroom (by Term)
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/academic/terms/{{termId}}/classrooms"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "มัธยมศึกษาปีที่ 5/2"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
