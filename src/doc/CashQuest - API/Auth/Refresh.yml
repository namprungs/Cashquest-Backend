info:
  name: Refresh
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/auth/refresh"
  headers:
    - name: x-refresh-key
      value: "{{refreshKey}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
