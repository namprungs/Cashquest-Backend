info:
  name: Get TermStageRules by Term
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/academic/terms/{{termId}}/stage-rules"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
