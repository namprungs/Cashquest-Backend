info:
  name: Get My Profile
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/me/profile?termId={{termId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
