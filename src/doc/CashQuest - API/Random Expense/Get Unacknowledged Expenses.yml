info:
  name: Get Unacknowledged Expenses
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/expenses/unacknowledged?termId={{termId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: termId
      value: "{{termId}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
