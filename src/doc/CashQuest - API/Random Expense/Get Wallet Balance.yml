info:
  name: Get Wallet Balance
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/expenses/wallet-balance?termId={{termId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: termId
      value: "{{termId}}"
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
