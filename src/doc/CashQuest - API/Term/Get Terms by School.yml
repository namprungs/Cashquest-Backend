info:
  name: Get Terms by School
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/academic/schools/{{schoolId}}/terms?status=DRAFT"
  params:
    - name: status
      value: DRAFT
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
