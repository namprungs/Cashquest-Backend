info:
  name: List Quizzes
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/quizzes?termId={{termId}}&moduleId={{moduleId}}&search=math"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: termId
      value: "{{termId}}"
      type: query
    - name: moduleId
      value: "{{moduleId}}"
      type: query
    - name: search
      value: math
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
