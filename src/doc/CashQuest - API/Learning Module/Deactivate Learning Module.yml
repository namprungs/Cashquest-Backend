info:
  name: Deactivate Learning Module
  type: http
  seq: 6

http:
  method: PATCH
  url: "{{baseUrl}}/learning-modules/{{moduleId}}/deactivate"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
