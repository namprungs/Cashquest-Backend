info:
  name: Delete Learning Module
  type: http
  seq: 7

http:
  method: DELETE
  url: "{{baseUrl}}/learning-modules/{{moduleId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
