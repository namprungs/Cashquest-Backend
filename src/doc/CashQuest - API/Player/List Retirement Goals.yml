info:
  name: List Retirement Goals
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/player/terms/{{termId}}/retirement-goals"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
