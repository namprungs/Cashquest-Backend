info:
  name: Get LifeStages
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/academic/life-stages"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
