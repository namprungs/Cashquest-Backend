info:
  name: Get School By ID
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/academic/schools/{{schoolId}}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
