info:
  name: Update School
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/academic/schools/{{schoolId}}"
  headers:
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |-
      {
        "name": "Chula Updated",
        "plan": "ENTERPRISE"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
