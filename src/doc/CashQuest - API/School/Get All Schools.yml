info:
  name: Get All Schools
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/academic/schools"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
