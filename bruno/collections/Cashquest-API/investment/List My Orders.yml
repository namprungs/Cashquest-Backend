info:
  name: List My Orders
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/market/terms/{{termId}}/orders/me"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

runtime:
  variables:
    - name: termId
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
