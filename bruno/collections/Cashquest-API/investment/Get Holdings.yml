info:
  name: Get Holdings
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/market/terms/{{termId}}/holdings/me"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

runtime:
  variables:
    - name: termId
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
