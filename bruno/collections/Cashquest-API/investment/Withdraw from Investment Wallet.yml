info:
  name: Withdraw from Investment Wallet
  type: http
  seq: 14

http:
  method: POST
  url: "{{baseUrl}}/market/terms/{{termId}}/investment-wallet/withdraw"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "amount": 500
      }
  auth: inherit

runtime:
  variables:
    - name: termId
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
