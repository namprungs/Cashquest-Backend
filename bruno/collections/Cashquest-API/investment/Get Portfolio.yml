info:
  name: Get Portfolio
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/market/terms/{{termId}}/portfolio/me"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

runtime:
  variables:
    - name: termId
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
