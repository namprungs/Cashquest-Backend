info:
  name: Delete School
  type: http
  seq: 6

http:
  method: DELETE
  url: "{{baseUrl}}/academic/schools/{{id}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

runtime:
  variables:
    - name: id
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
