info:
  name: List Life Stages
  type: http
  seq: 10

http:
  method: GET
  url: "{{baseUrl}}/academic/life-stages"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
