info:
  name: List Schools
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/academic/schools"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
