info:
  name: Publish Quest
  type: http
  seq: 16

http:
  method: POST
  url: "{{baseUrl}}/quests/{{questId}}/publish"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

runtime:
  variables:
    - name: questId
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
