info:
  name: Get Interactive Quest Status
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/quests/{{questId}}/interactive-status"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

runtime:
  variables:
    - name: questId
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
