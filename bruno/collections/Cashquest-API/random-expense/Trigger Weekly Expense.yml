info:
  name: Trigger Weekly Expense
  type: http
  seq: 4

http:
  method: POST
  url: "{{baseUrl}}/expenses/trigger"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  body:
    type: json
    data: |-
      {
        "termId": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
