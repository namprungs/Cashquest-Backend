info:
  name: List Learning Modules
  type: http
  seq: 4

http:
  method: GET
  url: "{{baseUrl}}/learning-modules"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
