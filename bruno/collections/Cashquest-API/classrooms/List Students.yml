info:
  name: List Students
  type: http
  seq: 7

http:
  method: GET
  url: "{{baseUrl}}/academic/classrooms/{{classroomId}}/students"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

runtime:
  variables:
    - name: classroomId
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
