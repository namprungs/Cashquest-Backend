info:
  name: Delete Quiz
  type: http
  seq: 2

http:
  method: DELETE
  url: "{{baseUrl}}/quizzes/{{quizId}}"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  auth: inherit

runtime:
  variables:
    - name: quizId
      value: ""

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
