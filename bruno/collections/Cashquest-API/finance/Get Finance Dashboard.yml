info:
  name: Get Finance Dashboard
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/me/finance"
  headers:
    - name: Authorization
      value: Bearer {{accessToken}}
  params:
    - name: termId
      value: ""
      type: query
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
